import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    checkboxcontainer: {
        flexDirection: "row",
    },
    checkbox: {
       marginTop:24,
    },
    checkboxtext: {
        fontSize: 25,
        marginTop:25,
        fontWeight:"700",
    },
});
