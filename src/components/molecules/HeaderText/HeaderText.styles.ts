import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
      headerTextContainer:{
        gap:5,
        alignSelf: "flex-start",
        paddingLeft:15,
        height:"auto",
        marginBottom:25,
      },
});
